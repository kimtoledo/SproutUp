// Extends `expect` with @testing-library/jest-dom matchers (e.g. toBeInvalid,
// toHaveAccessibleName). This only registers matchers; it is inert for the
// node-environment logic tests that never touch the DOM.
import '@testing-library/jest-dom/vitest';
