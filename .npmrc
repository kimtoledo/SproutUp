save-exact=true
engine-strict=true
fund=false
# npm Arborist currently crashes while resolving Better Auth/Vitest optional peers.
# The lockfile plus CI checks remain authoritative until the upstream resolver is fixed.
legacy-peer-deps=true
